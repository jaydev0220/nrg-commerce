{
	"name": "@apps/contact-worker",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler types && wrangler deploy --dry-run --outdir build",
		"deploy": "wrangler deploy",
		"check": "wrangler types && tsc --noEmit --project tsconfig.json",
		"format": "prettier --ignore-path ../../.prettierignore --write .",
		"lint": "prettier --ignore-path ../../.prettierignore --check . && eslint .",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"types": "wrangler types"
	},
	"dependencies": {
		"@packages/schemas": "workspace:*"
	},
	"devDependencies": {
		"@eslint/js": "catalog:lint",
		"@cloudflare/vitest-pool-workers": "catalog:test",
		"@packages/eslint-config": "workspace:*",
		"@packages/prettier-config": "workspace:*",
		"@packages/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"@vitest/coverage-istanbul": "catalog:test",
		"eslint": "catalog:lint",
		"eslint-config-prettier": "catalog:lint",
		"prettier": "catalog:lint",
		"tsx": "catalog:",
		"typescript": "catalog:",
		"typescript-eslint": "catalog:lint",
		"vitest": "catalog:test",
		"wrangler": "catalog:svelte"
	}
}
